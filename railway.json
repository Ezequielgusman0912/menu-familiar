{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn family_menu.wsgi --bind 0.0.0.0:$PORT --log-file -",
    "healthcheckPath": "/"
  }
}
